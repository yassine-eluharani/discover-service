name: applypilot-discovery

services:
  discovery:
    image: ghcr.io/yassine-eluharani/applypilot-discovery:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
